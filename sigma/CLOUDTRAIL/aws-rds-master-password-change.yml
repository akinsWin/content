title: AWS RDS Master Password Change
author: faloker
detection:
- SELECT * FROM CLOUDTRAIL WHERE ($EventSource = "rds.amazonaws.com" AND $ResponseElementsPendingModifiedValuesMasterUserPassword
  LIKE "%" AND $EventName = "ModifyDBInstance")
