title: Ping Hex IP
author: Florian Roth
detection:
- SELECT * FROM WINDOWSPROCESS_CREATION WHERE (($CommandLine LIKE "%\\ping.exe 0x%"
  OR $CommandLine LIKE "%\\ping 0x%") AND ($Image LIKE "%ping.exe%"))
