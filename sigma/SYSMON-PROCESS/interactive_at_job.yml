title: Interactive AT Job
author: E.M. Anhaus (orignally from Atomic Blue Detections, Endgame), oscd.community
detection:
- SELECT * FROM SYSMON-PROCESS WHERE ($Image LIKE "%\\at.exe" AND $CommandLine
  LIKE "%interactive%")
