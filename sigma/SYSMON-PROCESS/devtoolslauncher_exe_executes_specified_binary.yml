title: Devtoolslauncher.exe Executes Specified Binary
author: Beyu Denis, oscd.community (rule), @_felamos (idea)
detection:
- SELECT * FROM SYSMON-PROCESS WHERE ($Image LIKE "%\\devtoolslauncher.exe"
  AND $CommandLine LIKE "%LaunchForDeploy%")
