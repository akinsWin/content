title: Stop Windows Service
author: Jakob Weinzettl, oscd.community
detection:
- SELECT * FROM SYSMON-PROCESS WHERE (($Image LIKE "%\\sc.exe" OR $Image
  LIKE "%\\net.exe" OR $Image LIKE "%\\net1.exe") AND $CommandLine LIKE "%stop%")
