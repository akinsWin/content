title: Possible Process Hollowing Image Loading
author: Markus Neis
detection:
- SELECT * FROM WINDOWSIMAGE_LOAD WHERE (($Image LIKE "%\\notepad.exe") AND ($ImageLoaded
  LIKE "%\\samlib.dll" OR $ImageLoaded LIKE "%\\WinSCard.dll"))
