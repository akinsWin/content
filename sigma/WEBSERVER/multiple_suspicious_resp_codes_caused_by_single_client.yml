title: Multiple Suspicious Resp Codes Caused by Single Client
author: Thomas Patzke
detection:
- SELECT $SrcIP, count(*) AS agg FROM WEBSERVER WHERE $ReturnCode IN ("400",
  "401", "403", "500") GROUP BY $SrcIP HAVING agg > 10
