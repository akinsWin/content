id: ae01a843f5644551bced9aceff7fd00e
name: Script Execution via WMI
path: /dnif
description: ''
type: standard
author: DNIF
blocks:
- type: sql
  query: SELECT $Image,$User,$ParentCommandLine,$ParentImage,$CommandLine, COUNT ($CommandLine)
    AS PCOUNT FROM SYSMON-PROCESS WHERE $Image LIKE "%scrcons.exe"  GROUP BY $Image,$ParentImage,$ParentCommandLine,$CommandLine,$User
    LIMIT 100
- type: signal
  DetectionName: Script Execution via WMI
  DetectionTactic: Execution
  DetectionTechnique: Windows Management Instrumentation
  TargetHost: $System
  DetectionScore: 5
  DetectionConfidence: Low
